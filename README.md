# Mini-SQL-Database-for-Student-Records
Built a simple SQL database including tables, constraints, and queries for storing academic records. Practiced SELECT, JOIN, GROUP BY, and basic normalization concepts.
